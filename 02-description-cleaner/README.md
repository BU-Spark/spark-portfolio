# Stage 2 — Description Cleaner

Strips admin boilerplate from raw `description_text` before LLM ingestion.

See `clean_descriptions.py` (stub) and `../PIPELINE.md` for details.

**Status:** stub only — TODO implement boilerplate stripping.

**Input:** `../output/<year>/projects.json`
**Output:** `../output/<year>/projects_cleaned.json`
