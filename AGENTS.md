# AGENTS.md — spark-portfolio

Monorepo for the Spark Project Portfolio program. See `README.md` for the layout and
the "one program, shared spine" model.

- Work in the subdirectory for your initiative (`spine/`, `hub/`, `drive-walker/`,
  `semantic-extraction/`).
- Integration is via the shared `spine/` schema — coordinate changes there.
- Move existing code for your piece into its subdirectory; don't leave it scattered.
